# Orchestrator Handoff

## Identity

- **repo**: `telegram-local-ingest`
- **path**: `/home/tony/workspace/telegram-local-ingest`
- **current iteration**: `iter-1`
- **current sprint**: `sprint-12-utility-cleanup-polish`
- **harnessVersion**: `1.6.11`

## Status

### Harness Adoption Repair — 2026-04-24

This existing downstream repository has now been adopted into the `/vibe-init` state without rewriting product code. The agent-safe bootstrap command `npm run vibe:init -- --from-agent-skill` was run in this workspace and created `.vibe/config.local.json` with the default local provider role settings. `.vibe/config.json` now carries explicit project identity for `telegram-local-ingest`; `.vibe/agent/sprint-status.json`, this handoff, `session-log.md`, and `docs/context/product.md` are project-owned downstream state rather than copied `vibe-doctor` template state.

The adoption pass intentionally avoided product runtime fixes. Product review items to keep visible for separate product work or validation are PDF `pdftotext` readiness/setup/docs, PDF/DOCX output policy alignment, and DOCX XML sanitizing for extracted text. The current working tree already contained product changes around those areas before this adoption pass; keep them separated from harness adoption when reviewing or committing.

The repository has been created from `vibe-doctor`. Sprint 0 through Sprint 8 are complete. The project context now targets a Telegram Local Bot API Server based local ingest worker, and the code has config loading, Telegram Bot API baseline, startup health checks, SQLite-backed operational state, Telegram capture-to-job creation, controlled Telegram file import into runtime staging/archive, immutable Obsidian raw bundle writing, RTZR STT client/helpers, a protected wiki ingest adapter boundary, Telegram operator command primitives, and an integrated worker dispatch loop.

The harness has been synced from the local WSL-visible `vibe-doctor` template at `/mnt/c/Users/Tony/Workspace/vibe-doctor` to `1.5.15`. The Telegram-specific `.env.example` was preserved after sync because the upstream harness template is provider-only. The v1.5.x sync line includes the WSL-safe Codex wrapper fix, project-safe `.gitignore` line merge, cross-platform Node statusline wiring, Codex skill wrappers, harness-only typecheck isolation via `tsconfig.harness.json`, upstream bootstrap/sync hardening, and direct-shell `/vibe-init` guard. A live smoke readiness checker exists at `apps/worker/src/readiness.ts` and can be run with `npm run smoke:ready`.

Live smoke has passed against the local Telegram Bot API Server and temporary vault. `TonyYoniBot` accepted an `/ingest project:smoke tag:live first smoke test` file message, created SQLite job `tg_5985744318_2`, imported the Local Bot API file into runtime archive/staging, wrote immutable raw bundle `raw/2026-04-22/tg_5985744318_2/`, skipped RTZR/wiki because credentials/adapter are not configured, and completed with Telegram notification.

Upload-only behavior is now supported: an authorized file upload creates an ingest job even without `/ingest` text. Plain captions are preserved as instructions; `/ingest` captions still provide optional `project:`/`tag:` metadata. After a job reaches `COMPLETED`, the worker deletes the corresponding Telegram Local Bot API Server source file from `TELEGRAM_LOCAL_FILES_ROOT` so `documents/file_*` does not duplicate the archived/runtime/raw copies. A Windows launcher exists as `Start Telegram Local Ingest.cmd` and has also been copied to `C:\Users\Tony\Desktop\Start Telegram Local Ingest.cmd`; it starts the local Bot API server and worker in separate WSL console windows using the project `.env`.

Operational hardening started: `scripts/start-local-stack.sh`, `scripts/stop-local-stack.sh`, and `scripts/restart-local-stack.sh` now manage the local Bot API server and worker with pid files and logs under `runtime/pids` and `runtime/logs`. The desktop launcher now calls `scripts/start-local-stack.sh` instead of embedding long server commands. Worker logs now include update receipt, queued job, bundle path, cleanup result, and completion lines. Upload UX now includes uploaded filenames in queued/completed Telegram messages.

Bot responses are now Korean-first and include suitable emoji in operator/status, queued/completed/failure, and STT context flows. Audio/voice uploads are held in `RECEIVED` until the operator selects a recording environment and recognition language with inline buttons. The final selection is stored as a `stt.preset_selected` job event with the provider config, language/model branch, and translation default relation, then the job transitions to `QUEUED`. Raw bundle manifests include `processing_context.stt_preset`, selected language/model details, and `processing_context.translation.default_relation` so later STT/translation/wiki processing can reuse the captured context. `TRANSLATION_DEFAULT_RELATION` defaults to `business` and is exposed in `.env.example` for easy future admin/env management.

RTZR STT is now wired into the worker. When `RTZR_CLIENT_ID` and `RTZR_CLIENT_SECRET` are configured, audio/voice files are normalized if needed with ffmpeg, submitted to RTZR using the selected preset config, and written as `*.rtzr.json` plus `*.transcript.md` artifacts under raw bundle `extracted/`. The artifact paths are recorded in `rtzr.transcribed` job events before bundle writing, so `BUNDLE_WRITING` can reconstruct extracted artifacts after a restart. If RTZR credentials are not configured, audio jobs still complete with `rtzr.skipped` and preserve the raw original bundle.

Audio/voice uploads now collect STT context in two steps: first recording environment, then recognition language. The language choice is stored with the final `stt.preset_selected` event and raw bundle manifest. RTZR model config branches automatically: Korean/Japanese use `sommers`, while English/Chinese/mixed use `whisper`; mixed uses `language=multi` and `language_candidates=["ko","en","ja","zh"]`.

SenseVoice local CPU STT is available as an on-demand provider. Set `STT_PROVIDER=sensevoice`, run `scripts/setup-sensevoice-cpu.sh`, and point `SENSEVOICE_PYTHON` to `.venv-sensevoice/bin/python`. The worker does not start a persistent SenseVoice server; it spawns `scripts/sensevoice-transcribe.py` only while an audio/voice job is being processed. SenseVoice artifacts are written as `*.sensevoice.json` plus `*.transcript.md` under raw bundle `extracted/`, and the selected STT preset plus SenseVoice config are preserved in `processing_context.stt_preset`.

`scripts/setup-sensevoice-cpu.sh` now prewarms the SenseVoice model cache by default through `scripts/sensevoice-prewarm.py`. This avoids the first live Telegram audio job blocking on ModelScope download. Set `SENSEVOICE_PREWARM=0` only when intentionally skipping model download during setup.

Worker logs now use uppercase bracketed task tags, for example `[WORKER] ... [SENSEVOICE]`, `[IMPORT]`, `[STT]`, `[BUNDLE]`, and `[CLEANUP]`. SenseVoice helper progress is phase-based and streamed into the worker log while a job is running: START, CONFIG, THREADS, IMPORT, MODEL_LOAD, TRANSCRIBE, POSTPROCESS, WRITE_OUTPUT, DONE, or ERROR with approximate progress percentages.

Failed Telegram job notifications now include an inline `🔁 다시 처리` button. The button callback validates the job's chat/user ownership, only accepts `FAILED` jobs, records `job.retry_requested`, transitions the job back to `QUEUED`, and sends a Korean confirmation message. The existing `/retry <job_id>` command remains available.

The Windows desktop launcher now behaves as a start/stop toggle. It calls `scripts/local-stack-status.sh` first; if both managed processes are already running, it asks whether to stop them and runs `scripts/stop-local-stack.sh` on `Y`. If only one managed process is running, it asks whether to stop the partial stack; otherwise it continues with `scripts/start-local-stack.sh`. The desktop copy at `C:\Users\Tony\Desktop\Start Telegram Local Ingest.cmd` was updated.

Symlink review for raw originals: do not symlink `raw/**` to Telegram Local Bot API Server storage. Those paths contain bot-token-derived directories, are intentionally deleted after completion, and make raw bundles non-portable. Keep raw bundle originals as copied, self-contained files; if links are needed later, use Obsidian-relative links to files already inside the finalized raw bundle.

The post-processing utility roadmap is now documented in `docs/context/product.md`, `docs/context/architecture.md`, and `docs/plans/sprint-roadmap.md`. It keeps the Telegram/raw/wiki ingest pipeline as the durable source of truth, then adds deterministic preprocessing, language/translation-needed checks, local agent-based translation/formatting, runtime-only downloadable outputs, and later utility-bot separation. Personal OAuth-backed Codex/Claude Code usage is documented as operator-only; any public, paid, or multi-user service variant must move to official API credentials.

Sprint 9 output delivery is complete. SQLite schema version 2 adds `job_outputs`; `packages/output-store` copies generated files into `runtime/outputs/<job_id>/<output_id>/`, records 24-hour expiry metadata, resolves active/expired/deleted state, and cleans up expired runtime files without touching `raw/**` or `wiki/**`. `packages/telegram` now supports multipart `sendDocument`, and the worker handles `download:<output_id>` inline callbacks with Telegram allowlist and job chat/user ownership validation before sending an active output file. Callback acknowledgements are non-fatal; if Telegram reports an expired/invalid callback query, the worker logs `[TELEGRAM]` and still sends the document when access checks pass.

Sprint 10 preprocessing and language detection is complete. `packages/preprocessors` collects text-like imported originals, extracts DOCX text into runtime-only `runtime/extracted/<job_id>/preprocess/` artifacts, and reads bundled `*.transcript.md` files. It strips transcript Markdown boilerplate before scoring and records metadata without duplicating source text into SQLite. `packages/language-detector` provides deterministic Korean/English/Chinese/Japanese/mixed/unknown scoring and translation-needed decisions. The worker now records `preprocess.completed` and `language.detected` job events during `INGESTING`, using `TRANSLATION_TARGET_LANGUAGE` from config, which defaults to `ko`.

Sprint 11 is complete. `packages/agent-adapter` builds a job-scoped prompt with explicit raw/output hard boundaries, supports `{projectRoot}`, `{promptFile}`, `{outputDir}`, `{bundlePath}`, and `{jobId}` command placeholders or stdin prompt delivery, writes prompt/work files outside the output deliverable directory, and snapshots `raw/**` before/after execution so raw mutations fail the adapter. During `INGESTING`, the worker runs the agent only when `language.detected.translationNeeded=true`, registers generated downloads through `packages/output-store`, and sends a Korean completion message with `download:<output_id>` buttons for 24-hour downloads. DOCX/HWP-family sources are delivered as document files, while non-DOCX/HWP sources are delivered as source-stem `_translated.pdf` files. `scripts/run-codex-postprocess.sh` is the operator-ready Codex wrapper, `scripts/run-claude-postprocess.sh` is the operator-ready Claude Code wrapper, and `scripts/smoke-agent-postprocess.sh` provides readiness/live smoke entrypoints for both providers. `AGENT_POSTPROCESS_PROVIDER` defaults to `none`; enabling `codex`, `claude`, or `custom` requires `AGENT_POSTPROCESS_COMMAND`.

Sprint 12 is implementation-complete except live validation. Hidden output lifecycle callbacks now exist for future utility-bot UX: `output-discard:<output_id>` deletes the runtime output file and marks the output deleted, while `output-regenerate:<output_id>` records `output.regenerate_requested` as an interface-only event without exposing a button or re-running the agent. The output polish slice shows the real KST expiry deadline in both message body and download button. Non-DOCX/HWP uploads still emit mobile-friendly PDFs with a Korean/CJK-capable font when available. DOCX/PDF/EML/HWP-family uploads use DOCX as the editable delivery baseline, but agents are now Markdown/JSON translation workers: they must create `translated.md`, must create `translations.json` when a DOCX block structure is provided, and must not create DOCX/PDF/HWP/HWPX/ZIP/binary deliverables. For uploaded DOCX sources, preprocessing writes deterministic body paragraph block ids; the worker clones the original DOCX package, replaces translated body paragraph text inside `word/document.xml`, appends `[원문]`, validates the DOCX package, and keeps final naming under `<source-stem>_translated.docx`. This preserves the source template/package better than `pandoc --reference-doc`; if `translations.json` is missing or invalid, the worker falls back to the existing `pandoc` render path. PDF/EML/HWP-family sources still render DOCX from translated Markdown plus extracted source text because there is no editable source DOCX template to clone. Non-DOCX/HWP PDF downloads use `<source-stem>_translated.pdf`. The worker snapshots preprocessed source sections before agent execution, removes intermediate agent files like `translated.md` and `translations.json` from `runtime/agent-postprocess/.../outputs`, and treats the worker as the sole owner of `[원문]` source-section composition so final output directories only keep normalized deliverables. `npm run setup:docx-rendering` now installs/checks pandoc, poppler PDF tools, tesseract, and default OCR language packs. The agent prompt includes the fixed Business Document Translation Preset but forbids agent-side DOCX/PDF/HWP/HWPX/ZIP/binary creation. Expired output cleanup now runs on worker startup and then on `WORKER_OUTPUT_CLEANUP_INTERVAL_MS`, `/status` shows output downloadability/expiry/discard state, and operations docs record the personal-OAuth-only boundary plus official API migration path for future public or multi-user service use. User-facing regenerate/discard Telegram controls are documented as a low-priority follow-up after the main upload, post-process, and download flow is stable.

OCR preprocessing is now implemented without changing language presets. `packages/preprocessors` uses `pdftotext` for text-layer PDFs, falls back to `pdftoppm` plus `tesseract` for scanned PDFs, and OCRs supported image uploads directly through `tesseract`. Readiness/setup docs now cover `PDFTOPPM_BIN`, `TESSERACT_BIN`, `OCR_LANGUAGES`, `OCR_PSM`, `PDF_OCR_MAX_PAGES`, and `PDF_OCR_DPI`; default OCR languages remain aligned with the current Korean/English/Chinese/Japanese scope.

Sprint 13 is now planned as `sprint-13-vault-reconcile-retention`. This captures the deletion/desync design before Sprint 12 is closed: managed delete should be the normal path for source/output/wiki cleanup, manual Obsidian/raw/wiki deletion should be treated as drift, LLMwiki lint should cover wiki graph health only, and SQLite synchronization must be handled by a deterministic worker/CLI reconcile command with tombstone state rather than by an LLM.

The worker now separates Telegram polling/callback handling from long-running job processing. SQLite schema version 3 adds `job_claims`, and the production loop claims runnable jobs into a bounded background worker pool so uploads, status/retry/cancel, STT preset callbacks, and download buttons remain responsive while Codex/RTZR/SenseVoice work continues. Concurrency is environment-driven: `WORKER_JOB_CONCURRENCY` defaults to `2`, `WORKER_STT_CONCURRENCY` defaults to `1`, `WORKER_AGENT_CONCURRENCY` defaults to `1`, `WORKER_JOB_CLAIM_TTL_MS` defaults to `7200000`, and `WORKER_OUTPUT_CLEANUP_INTERVAL_MS` defaults to `600000`. Keep agent concurrency at `1` for personal OAuth CLI stability unless explicitly testing parallel local agent sessions.

Live 10-file upload investigation on 2026-04-26 confirmed that Telegram/local capture created 10 separate one-file jobs, but the current local worker only processed two jobs at a time because `.env` has no `WORKER_*_CONCURRENCY` overrides and the defaults above applied. Translation-needed Codex post-processing then serialized behind `WORKER_AGENT_CONCURRENCY=1`. The worker now only acquires the agent semaphore for jobs that actually need an enabled agent; translation-not-needed jobs no longer wait behind long Codex jobs. Existing worker processes must be restarted to pick up this code change. To intentionally run up to 10 parallel file jobs locally, set `WORKER_JOB_CONCURRENCY=10` and `WORKER_AGENT_CONCURRENCY=10` in local `.env` before restarting; choose `WORKER_STT_CONCURRENCY` separately based on CPU/API capacity.

Claude Code post-processing is now wired for live testing. `AGENT_POSTPROCESS_PROVIDER=claude` is accepted by config, readiness detects `scripts/run-claude-postprocess.sh`, and the smoke script supports `--provider claude`. The Claude wrapper runs `claude --print` from the job-scoped agent workspace, adds only the agent workspace, raw bundle, and output directory as allowed directories, and relies on the same raw snapshot guard as Codex. Local `.env` currently points agent post-processing at the Claude wrapper, and the local stack was restarted after readiness showed `Claude postprocess wrapper: claude-cli 2.1.117 (Claude Code)`. Use `npm run smoke:agent:ready:claude` for CLI health and `npm run smoke:agent:live:claude` for a temporary raw-bundle/output write test.

EML uploads now use the same document post-processing path as DOCX/PDF/HWP-family sources. `.eml` files and `message/rfc822` uploads are preserved as raw originals, parsed into runtime-only `eml_text` preprocessing artifacts with decoded `Subject`, `From`, `To`, `Cc`, `Date`, and body text, and then passed through the existing language detection and configured agent postprocess flow. Multipart messages skip attachments and prefer `text/plain` over `text/html`; the worker normalizes translated EML deliverables to `<source-stem>_translated.docx` and appends the extracted original mail text under the existing `[원문]` source section. The local stack was restarted after this change, and readiness reports worker concurrency at jobs=10/stt=10/agent=10 with RTZR STT, Claude postprocess, pandoc, PDF tools, and OCR tools ready.

The async worker rollout exposed one important concurrency edge: raw snapshot guards in `packages/agent-adapter` and `packages/wiki-adapter` must scope to the current job bundle rather than the entire `raw/**` tree, otherwise a sibling job can trigger false `modified raw files` failures while writing its own raw bundle. That is now fixed. Retries that re-enter `BUNDLE_WRITING` also now reuse an existing finalized source bundle when one already exists for the job, so post-process failures can be retried without tripping `Raw bundle already exists and will not be overwritten`. STT logging was also tightened so non-audio jobs log `STT skipped ... reason=no_audio_files` instead of looking like speech transcription actually ran.

Claude Code live post-processing exposed a permission mismatch in job `tg_5985744318_161`: the agent prompt referenced prepared `.txt` and `.blocks.json` artifacts under `runtime/extracted/...`, but the Claude wrapper only allowed the job-scoped agent workspace, raw bundle, and output directory. `packages/agent-adapter` now stages prepared preprocessing artifacts into `.agent-work/.../artifacts/` before prompt generation, so both Claude and Codex see prompt paths inside the allowed workspace while the worker still owns preprocessing and output rendering. Structured DOCX jobs still require `translated.md` plus `translations.json`; worker diagnostics record stdout/stderr when an agent exits without usable output files.

Template-preserved DOCX output now keeps the agent's support material. `translations.json` remains the source for replacing original DOCX body blocks inside the uploaded package/template, while `translated.md` is parsed for translation metadata, glossary/terminology sections, and translator notes. Those support sections are inserted around the preserved DOCX body before the worker appends `[원문]`, so the final document keeps both the source template and the review-friendly translation context.

## Durable Decisions

- Use Telegram as the single capture, command, and notify channel.
- Use Telegram Local Bot API Server for large files.
- Do not include Dropbox in V1.
- Use npm workspaces initially for harness compatibility.
- Use deterministic TypeScript code for queue, state, file import, retry, permissions, and notifications.
- Use `packages/db` as the SQL boundary; app code should call repository functions rather than writing SQL inline.
- Use `packages/capture` as the Telegram update-to-job orchestration boundary.
- Use `packages/importer` as the Telegram file import boundary; never process files in-place from Bot API storage.
- Use `packages/vault` as the immutable raw bundle writing boundary.
- Use `packages/rtzr` as the RTZR STT boundary for auth, submit/poll, transcript artifacts, and ffmpeg conversion helpers.
- Use `packages/sensevoice` as the on-demand local SenseVoice STT boundary; invoke it per job rather than as a resident service.
- Use `packages/wiki-adapter` as the LLM/wiki command boundary, guarded by a write lock and raw snapshot checks.
- Use `packages/output-store` as the runtime-only output delivery boundary; output files are TTL cache artifacts, not source-of-truth vault content.
- Use `packages/preprocessors` and `packages/language-detector` for deterministic pre-agent text collection and translation-needed checks.
- Use `packages/agent-adapter` as the local agent command boundary; adapter workspaces may write outputs only and must not mutate `raw/**`.
- Use `packages/operator` for Telegram status/retry/cancel and notification/report text.
- Keep agents as Markdown/JSON translation workers for document post-processing; worker-owned renderers create and validate DOCX/PDF deliverables, and uploaded DOCX sources use block-id `translations.json` replacement to preserve the original package/template when possible.
- Use RTZR STT for audio and voice files.
- Write immutable Obsidian raw bundles under `raw/<date>/<source_id>/`.
- Let the LLM wiki adapter update `wiki/**` only, never `raw/**`.

## Next Action

MVP roadmap plus Sprint 9, Sprint 10, and Sprint 11 utility foundations are complete. Sprint 12 implementation is complete except live validation; local `.env` is already set for an explicit 10-file parallel test. Next step is to repeat the live multi-file upload while a long post-process job is running to confirm polling/download callbacks and translation-skipped jobs stay responsive under the background worker pool. Also live retry a previously failed post-process job to confirm the bundle-reuse path behaves correctly in the real Telegram flow. After that, run one live DOCX upload to confirm `docx_text + .blocks.json -> translated.md + translations.json -> template-preserved <source-stem>_translated.docx -> [원문]`; one live EML upload to confirm `eml_text -> translated.md -> <source-stem>_translated.docx -> [원문]`; one scanned PDF or image upload to confirm OCR delivery; and `/status <job_id>` after active/expired/discarded outputs to confirm operator-facing output states. Regenerate/discard button exposure is a low-priority follow-up. Sprint 13 should follow with vault reconcile/retention implementation for managed delete, manual drift detection, LLMwiki lint integration, and SQLite tombstones. Real Telegram DOCX uploads on 2026-04-23 confirmed `docx_text -> language=en -> agent=codex -> output registration -> completion notification -> source cleanup`, and a later download button click confirmed live `sendDocument` delivery after the non-fatal callback acknowledgement fix.

```text
Current utility flow target: local Telegram server -> upload file -> SQLite job -> SQLite job claim -> bounded background processing pool -> import -> raw bundle -> preprocessing -> translation-needed check -> configured agent postprocess -> worker-normalized `<source-stem>_translated` deliverable -> worker-appended `[원문]` source section with translation first -> output store -> Telegram download button with actual expiry -> hidden output lifecycle callbacks.
```

## Continuation Prompt

Use this when resuming in a fresh session:

```text
Continue from /home/tony/workspace/telegram-local-ingest. Read .vibe/agent/handoff.md and .vibe/agent/session-log.md first. Harness is synced and the downstream project is initialized as `telegram-local-ingest`. Sprint 12 implementation is complete except live validation: hidden `output-discard:<output_id>` and `output-regenerate:<output_id>` callbacks exist, OCR preprocessing handles scanned PDFs/images through `pdftoppm` plus `tesseract`, EML preprocessing parses `.eml`/`message/rfc822` into runtime-only `eml_text`, agents are Markdown/JSON-only and must create `translated.md`, DOCX sources additionally require `translations.json` when `.blocks.json` exists, final document deliverables are rendered/validated by the worker and normalized to `<source-stem>_translated.*` with worker-owned `[원문]` composition, DOCX sources preserve the original package/template by replacing body paragraph text in `word/document.xml` when structured translations are valid, expired output cleanup runs on startup and then on `WORKER_OUTPUT_CLEANUP_INTERVAL_MS`, and `/status` reports downloadable/expired/discarded output state. Defaults: `WORKER_JOB_CONCURRENCY=2`, `WORKER_STT_CONCURRENCY=1`, `WORKER_AGENT_CONCURRENCY=1`, `WORKER_JOB_CLAIM_TTL_MS=7200000`, `WORKER_OUTPUT_CLEANUP_INTERVAL_MS=600000`; local `.env` is currently raised to jobs=10, stt=10, agent=10 and `AGENT_POSTPROCESS_PROVIDER=claude` with `scripts/run-claude-postprocess.sh`. The local stack was restarted after the worker-owned document changes and readiness passed with RTZR STT, Claude postprocess, pandoc, PDF tools, and OCR tools ready. `STT_PROVIDER=rtzr`, so STT concurrency 10 is API-backed; lower `WORKER_STT_CONCURRENCY` if switching to SenseVoice. `AGENT_POSTPROCESS_PROVIDER` supports `none`, `codex`, `claude`, and `custom`; use `npm run smoke:agent:ready:claude` and `npm run smoke:agent:live:claude` for Claude checks. Do not add Dropbox. Use Telegram Local Bot API Server for large files. Next step: live retry a failed post-process job, live-test responsiveness during a long post-process job, upload one DOCX, upload one EML, upload one scanned PDF or image for OCR, and check `/status <job_id>` across active/expired/discarded outputs before closing Sprint 12. Sprint 13 should implement vault reconcile/retention for managed delete, manual drift detection, LLMwiki lint integration, and SQLite tombstones.
```

## Latest Verification

- Latest verification after template-preserved DOCX metadata/glossary restoration: `npm run typecheck` passed; focused `test/agent-adapter.test.ts` and `test/worker.test.ts` passed (`33`); `npm run build` passed; full `npm test` passed (`438` passed, `1` skipped); `git diff --check` passed; UTF-8/mojibake checks passed; `npm run vibe:checkpoint` passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready.
- Latest verification after Claude prepared-artifact staging fix: `npm run typecheck` passed; focused `test/agent-adapter.test.ts` and `test/worker.test.ts` passed (`33`); `npm run build` passed; full `npm test` passed (`438` passed, `1` skipped); `git diff --check` passed; UTF-8/mojibake checks passed; `npm run vibe:checkpoint` passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready. Failed live job `tg_5985744318_161` was not automatically retried.
- Latest verification after DOCX template-preserved block replacement: `npm run typecheck` passed; focused `test/preprocessors.test.ts`, `test/agent-adapter.test.ts`, and `test/worker.test.ts` passed (`40`); `npm run build` passed; full `npm test` passed (`436` passed, `1` skipped); `git diff --check` passed; UTF-8/mojibake checks passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready; `npm run vibe:checkpoint` passed.
- Latest verification after structured DOCX prompt/empty-output diagnostics fix: `npm run typecheck` passed; focused `test/agent-adapter.test.ts` and `test/worker.test.ts` passed (`32`); `npm run build` passed; full `npm test` passed (`437` passed, `1` skipped); `git diff --check` passed; UTF-8/mojibake checks passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready; `npm run vibe:checkpoint` passed. Failed live job `tg_5985744318_161` was not automatically retried.
- Latest verification after Markdown-only agent / worker-owned document rendering: `npm run typecheck` passed; focused `test/agent-adapter.test.ts` and `test/worker.test.ts` passed (`30`); `npm run build` passed; full `npm test` passed (`435` passed, `1` skipped); `git diff --check` passed; UTF-8/mojibake checks passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready; `npm run vibe:checkpoint` passed.
- Latest verification after EML document postprocess support: `npm run typecheck` passed; focused `test/preprocessors.test.ts`, `test/agent-adapter.test.ts`, and `test/worker.test.ts` passed (`38`); `npm run build` passed; `git diff --check` passed; `npm run vibe:checkpoint` passed; `npm run ops:restart` passed and readiness reported jobs=10/stt=10/agent=10, RTZR, Claude postprocess, pandoc, PDF tools, and OCR tools ready.
- Latest verification after Sprint 12 cleanup/status polish: `npm run typecheck` passed; focused `test/telegram-local-baseline.test.ts`, `test/operator.test.ts`, `test/worker.test.ts`, and `test/live-smoke-readiness.test.ts` passed (`41`); `npm run build` passed; full `npm test` passed (`428` passed, `1` skipped); `git diff --check` passed.
- Latest verification after Claude Code agent postprocess wiring: `bash -n scripts/run-claude-postprocess.sh scripts/smoke-agent-postprocess.sh`, `npm run smoke:agent:ready:claude`, `npm run smoke:agent:live:claude`, `npm run smoke:ready`, `npm run typecheck`, focused `test/telegram-local-baseline.test.ts test/live-smoke-readiness.test.ts test/agent-adapter.test.ts` passed (`24`), `npm run build`, and `git diff --check` passed. Local stack was restarted and readiness reports `AGENT_POSTPROCESS_COMMAND` using `scripts/run-claude-postprocess.sh`.
- Latest verification after the 2026-04-26 multi-file concurrency investigation and agent-semaphore fix: `node --import tsx --test test/worker.test.ts` passed (`21`), and `npm run typecheck` passed.
- Latest verification after harness adoption repair: `npm run vibe:init -- --from-agent-skill` created `.vibe/config.local.json`; `npm run vibe:sync -- --force` completed against `v1.6.11` with backup `.vibe/sync-backup/2026-04-24T08-04-00-862Z`; `npm run typecheck` passed; `npm test` passed (`425` passed, `1` skipped); `npm run vibe:sync -- --dry-run` completed without init/template-state refusal; `node scripts/vibe-project-report.mjs --no-open --output /tmp/telegram-local-ingest-report.html` wrote a project report; `node .claude/statusline.mjs` showed `sprint-12-utility-cleanup-polish`; dashboard detach/start-stop worked on port `58175`; `npm run vibe:checkpoint` passed.
- Latest verification after OCR preprocessing: `bash -n scripts/setup-docx-template-rendering.sh`, `npm run typecheck`, focused preprocessor/worker/readiness tests passed (`31`), `npm run build` passed, and full `npm test` passed (`427` passed, `1` skipped).
- `npm run typecheck`: passed after harness sync and readiness checker
- `npm run build`: passed after harness sync and readiness checker
- App-focused tests passed: `47` passed, including `test/live-smoke-readiness.test.ts`
- `npm test`: passed after v1.5.3 harness sync (`311` passed, `0` failed); `test/run-codex-wrapper.test.ts` now passes under WSL
- `npm run smoke:ready`: passed after `.env` was configured
- Live smoke passed: job `tg_5985744318_2` completed and raw bundle was written under `/home/tony/obsidian-ingest-smoke-vault/raw/2026-04-22/tg_5985744318_2/`
- Latest app-focused verification after upload-only/source-cleanup/launcher changes: `npm run typecheck`, `npm run build`, and 50 focused app tests passed
- Latest app-focused verification after ops scripts/logging/UX changes: shell script syntax check, `npm run typecheck`, `npm run build`, and 50 focused app tests passed
- Latest verification after Korean bot responses and RTZR preset capture: `npm run typecheck` passed, `npm run build` passed, and focused app tests passed (`28` passed for Telegram capture/operator/worker/vault/config/live smoke readiness). Full `npm test` still has harness-only WSL failures in `test/run-codex-wrapper.test.ts`; application tests pass (`307` passed, `3` harness failures).
- Latest verification after RTZR STT worker wiring: `npm run typecheck` passed, `npm run build` passed, and focused app tests passed (`53` passed across DB/Telegram/importer/vault/RTZR/wiki/operator/worker/readiness). Full `npm test` still has only the known WSL `test/run-codex-wrapper.test.ts` failures (`308` passed, `3` failed).
- Latest verification after v1.5.15 harness sync: `npm run typecheck`, `npm run build`, and full `npm test` passed in WSL. Sync now includes Codex skill wrappers, Node statusline wiring, upstream bootstrap/sync hardening, harness-only typecheck isolation, and direct-shell `/vibe-init` guard.
- Latest verification after SenseVoice prewarm setup improvement: `npm run typecheck`, `npm run build`, app-focused tests passed (`57`), Python helper syntax checks passed, and full `npm test` passed (`320` passed, `0` failed).
- Latest verification after failed-job retry button: `npm run typecheck`, `npm run build`, and app-focused tests passed (`54`).
- Latest verification after RTZR language/model branching: `npm run typecheck`, `npm run build`, and app-focused tests passed (`54`).
- Latest verification after post-processing utility roadmap and Sprint 9 output delivery: `npm run typecheck`, `npm run build`, app-focused tests passed (`59`), and full `npm test` passed (`327`).
- Latest verification after Sprint 10 preprocessing/language detection: `npm run typecheck`, `npm run build`, app-focused tests passed (`66`), and full `npm test` passed (`334`).
- Latest verification after Sprint 11 worker-integrated agent post-processing: `npm run typecheck`, `npm run build`, app-focused tests passed (`74`), and full `npm test` passed (`370`).
- Latest verification after Codex postprocess wrapper/readiness and Sprint 12 hidden output lifecycle callbacks: shell syntax checks passed, `npm run typecheck`, `npm run build`, full `npm test` passed (`379`), and `npm run smoke:agent:ready` passed with `codex-cli 0.123.0`.
- Latest verification after Codex live smoke, DOCX preprocessing, shell-safe agent env command quoting, and detached local stack start: `npm run smoke:agent:live` passed with Codex `gpt-5.4`, `npm run typecheck`, `npm run build`, focused preprocessor/language/worker tests passed (`20`), and full `npm test` passed (`380`). Real Telegram upload job `tg_5985744318_24` reached upload/import/raw/cleanup; it skipped agent work before the DOCX fix because text extraction was missing.
- Latest verification after live DOCX Codex uploads and callback acknowledgement hardening: real Telegram jobs `tg_5985744318_27` and `tg_5985744318_29` produced `translated.md`, registered outputs, sent completion notifications, and cleaned source files. `npm run typecheck`, `npm run build`, and `node --import tsx --test test/worker.test.ts` passed.
- Latest verification after mobile PDF download output: `npm run typecheck`, `npm run build`, `node --import tsx --test test/worker.test.ts`, and full `npm test` passed (`381`).
- Latest verification after DOCX template-aware rendering: `bash -n scripts/setup-docx-template-rendering.sh`, `npm run typecheck`, `npm run build`, focused agent/readiness/worker tests passed (`23`), full `npm test` passed (`382`), and `npm run smoke:ready` passed with pandoc 3.1.3 plus LibreOffice 24.2.7.2.
- Latest verification after DOCX/HWP document-output policy and fixed business translation preset: `bash -n scripts/setup-docx-template-rendering.sh`, `npm run typecheck`, `npm run build`, focused agent/readiness/worker tests passed (`23`), full `npm test` passed (`382`), and `npm run smoke:ready` passed with pandoc 3.1.3.
- Latest verification after `[원문]` source labeling and source-stem download filenames: `npm run typecheck`, `npm run build`, focused agent/readiness/worker tests passed (`23`), full `npm test` passed (`382`), and `npm run smoke:ready` passed.
- Latest verification after bounded async worker processing and SQLite job claims: `npm run typecheck`, `npm run build`, focused DB/output-store/Telegram/readiness/worker tests passed (`37`), focused agent/output/readiness/worker tests passed (`27`), full `npm test` passed (`384`), and `npm run smoke:ready` passed with worker concurrency reported as jobs=2, stt=1, agent=1.
- Latest verification after worker-owned source composition and normalized translated output cleanup: `npm run typecheck`, `npm run build`, focused agent/output/readiness/worker tests passed (`27`), and focused DB/output-store/Telegram/worker tests passed (`37`).
- Latest verification after fixing parallel raw snapshot false positives, retry bundle reuse, and no-audio STT logs: escalated `node --import tsx --test test/agent-adapter.test.ts test/wiki-adapter.test.ts test/worker.test.ts` passed (`29`), and escalated `npm run build` passed.

## WSL Move Notes

- Active workspace moved to `/home/tony/workspace/telegram-local-ingest`.
- The old Windows workspace copy at `C:\workspace\telegram-local-ingest` is no longer present.
- The temporary local `origin` remote pointing to `/mnt/c/workspace/telegram-local-ingest` was removed; the WSL repo is self-contained.
- WSL Node installed with `nvm`: Node `v24.15.0`, npm `11.12.1`.
- WSL app verification passed: `npm run typecheck`, `npm run build`, and app-focused tests (`44` passed).
- Full `npm test` in WSL now passes after syncing local `vibe-doctor` v1.5.15.

## Links

- Product context: `docs/context/product.md`
- Architecture context: `docs/context/architecture.md`
- Roadmap: `docs/plans/sprint-roadmap.md`
